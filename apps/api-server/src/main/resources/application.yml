spring:
  application:
    name: copilot-api-server

server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true

copilot:
  security:
    tenant-context-required: true
